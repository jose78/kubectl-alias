---
version: v1
aliases:
  - name: ns
    sql: |
        SELECT p.metadata.name as name_pod, s.metadata.name as name_svc
        FROM 
            pod p, svc s
        where p.metadata.labels.app = s.spec.selector.app